import React from 'react'

function ShowErrorFormik(props) {
    return (
        <div>
            <p style={{color:'tomato',margin:'16px'}}>{props.children}</p>
        </div>
    )
}

export default ShowErrorFormik
